export function IskraMascot() {
  // Decorative inside controls/headings that already have an accessible name.
  // eslint-disable-next-line @next/next/no-img-element
  return <img className="iskra-mascot" src="/covers/iskra-mascot-64.webp" alt="" width={64} height={64} draggable={false} />;
}
