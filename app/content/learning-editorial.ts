import type { ProjectDefinition, QuestStep } from "./types";
import type { DataMode } from "../lib/preparation";

/** Keep IDs stable: editorial changes must not erase students' place in a lesson. */
export function editLearningJourney(project: ProjectDefinition, steps: QuestStep[], mode: DataMode): QuestStep[] {
  if (project.journey === "setup") return steps;
  return steps.map((original) => {
    let step = { ...original };
    const source = step.sourceStepId ?? step.id;
    if (project.kind === "advanced-site" && source === 1) {
      step = { ...step, title: "Сохраняем копию и выбираем одно улучшение", why: "У вас уже есть сайт. Сначала защитим рабочую версию, затем добавим нужную функцию — без повторной сборки с нуля.", action: "Откройте в Codex проект сайта с четвёртой недели. Отправьте команду ниже. Если сайта ещё нет, сначала пройдите «Первый сайт» в маршруте курса.", prompt: `Продолжаем мой существующий сайт, а не создаём новый. Сначала проверь, что в выбранной папке действительно мой сайт. Покажи его название и способ открыть предпросмотр. Создай безопасную резервную копию без секретов; не меняй рабочую публикацию. Предложи одно улучшение для задачи: ${project.outcome}. Уточни, какую функцию выбираю. Объясни, какие подключения и расходы понадобятся. До моего ответа ничего не подключай и не публикуй.`, expected: ["Открыт мой прежний сайт", "Рабочая версия сохранена отдельно", "Выбрано одно улучшение и понятны зависимости"], links: [{ label: "Вернуться к маршруту: сначала первый сайт", href: "?section=weeks" }], showScreenshot: false, guide: undefined };
    }
    if (project.kind === "simple-site" && project.slug !== "unique-design" && source === 13) {
      step = { ...step, title: "Собираем свой стиль из трёх примеров", why: "Не нужно придумывать дизайн с пустого листа. Возьмём удачные решения из разных примеров и соединим с вашим содержанием.", action: "Выберите три страницы в галерее. Из первой возьмите порядок блоков, из второй — настроение и шрифты, из третьей — одну деталь. Отправьте ссылки или скриншоты с пояснениями вместе с командой. Не копируйте чужие тексты, фотографии и логотипы.", prompt: `Помоги оформить мой сайт ${project.title}. Я пришлю три примера: 1 — структура блоков, 2 — настроение и типографика, 3 — одна деталь. Если ссылки не открываются, попроси скриншоты, не выдумывай их содержание. Соедини идеи с моими текстами и выбранной палитрой. Не клонируй чужой дизайн и не используй чужие изображения без разрешения. Сначала покажи короткое описание решения и один первый экран. После моего согласия примени его ко всей странице. Проверь читаемость и кнопки на телефоне.`, expected: ["Названо, что взято из каждого примера", "Сохранены мои тексты и выбранные цвета", "Первый экран и кнопки читаются на телефоне"], links: [{ label: "Галерея Landingfolio", href: "https://www.landingfolio.com/", external: true }, { label: "Подробный дополнительный квест про дизайн", href: "?quest=unique-design" }], showScreenshot: false, guide: undefined };
    }
    if (project.slug === "course-site" && step.journeyCheck === "student-access") {
      step = { ...step, title: "Проверяем, куда ведёт запись на курс", why: "В этом проекте делаем лендинг, а не закрытую школу. Посетитель должен понимать, куда попадёт после нажатия кнопки.", action: "Нажмите кнопку записи с телефона. Убедитесь, что открывается выбранный контакт или форма, а обещание на кнопке совпадает с результатом. Личный кабинет и доступ к урокам относятся к продвинутому проекту.", prompt: "Проверь кнопку записи на лендинге курса. Не добавляй авторизацию, оплату и закрытые уроки. Покажи адрес назначения, текст кнопки и поведение при ошибке. Не отправляй настоящий заказ и не списывай деньги.", expected: ["Кнопка ведёт по правильному адресу", "Посетитель понимает следующий шаг", "Нет обещания несуществующего кабинета"], showScreenshot: false, guide: undefined };
    }
    if (project.kind !== "portfolio" && source !== 17 && /клиентск.*коп|коп.*клиент|для заказчика/i.test(step.title)) {
      step = { ...step, title: `Дополнительно: ${step.title.toLowerCase()}`, why: "Сейчас мы выбираем, нужна ли дополнительная практика. Клиентскую копию достаточно полностью отработать один раз для сервиса, агента и сайта; в остальных квестах это дополнительная практика.", action: "Если вы уже делали клиентскую адаптацию этого типа, переходите дальше. Если это первая попытка, выполните команду и повторите проект с другими требованиями. Без реального заказчика подпишите результат «Учебный пример».", expected: ["Выбрано: выполнить адаптацию сейчас или вернуться позже", "Основная версия сохранена", "Учебная работа не выдана за заказ клиента"], showScreenshot: false };
    }
    const rules = "\n\nПРАВИЛА РАБОТЫ: продолжай текущий проект и используй уже подтверждённые ответы; не начинай опрос заново. Недостающие сведения спрашивай по одному. Файлы, таблицы и резервные копии создавай сам, не поручай ручную техническую подготовку ученице. Не придумывай реальные факты. В конце дай ссылку на результат или точный способ открыть его и одну короткую проверку. Не обещай, что отправка сообщения или подключение выполнены, без фактического подтверждения.";
    const modeRule = mode === "real" ? "РЕЖИМ РЕАЛЬНЫХ ДАННЫХ. Используй мои подтверждённые ответы. Если данных нет, спроси голосом или текстом; не подставляй учебные значения как настоящие.\n\n" : "РЕЖИМ УЧЕБНЫХ ДАННЫХ. Примеры — вымышленные, не выдавай их за мои настоящие данные.\n\n";
    return { ...step, prompt: step.prompt ? (step.prompt.includes("РЕЖИМ РЕАЛЬНЫХ") ? "" : modeRule) + step.prompt + rules : undefined, screenshotKind: step.screenshotKind ?? "prototype" };
  });
}
